# App store screenshots

This directory is reserved for store-ready device screenshots. Add captures using the platform/device names listed in `../metadata.json` (for example, `ios-6.7-inch-01.png` and `android-phone-01.png`).

The directory intentionally contains no fabricated screenshots yet; capture real app flows before submitting to TestFlight, App Store Connect, or Google Play.
